# @latimer-woods-tech/biome-config

Shared Biome config.

## Use

```jsonc
// biome.json
{ "extends": ["@latimer-woods-tech/biome-config/biome.json"] }
```

Single quotes · semicolons always · trailing commas all (JS/TS; JSON keeps `none`) · 100-col · 2-space indent · JSX/TSX supported.
