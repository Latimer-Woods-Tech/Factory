@adrper79-dot:registry=https://npm.pkg.github.com
@latimer-woods-tech:registry=https://registry.npmjs.org/
# Auth no longer needed: @latimer-woods-tech/* are public on registry.npmjs.org
# (kept for any other GitHub Packages scope; safe if unused)
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
