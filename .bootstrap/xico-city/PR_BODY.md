Full migration of `CANONICAL_DJMEXXICO_FeatureRegistry_v3.xlsx` (150 features across 29 slices). Section per slice.

IDs `XC-{SLICE}-{NNN}`. Weights per customer-impact rule. Internal `|` chars rendered as `&#124;` so the strict parser sees clean 11-cell rows.

All registry rows are currently PLANNED → Status 🔍.

**150 rows · 0 malformed · weighted baseline 0% (🔍).**
