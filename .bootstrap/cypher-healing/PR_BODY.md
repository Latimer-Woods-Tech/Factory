Complete handler enumeration from `src/index.ts` and `src/routes/{tenants,clients,bookings,courses}.ts`. No placeholder rows.

- Tenants (2), Clients (3), Bookings (4), Courses (4)
- Plus platform/auth/data/ops surface.

**36 rows · 0 malformed · weighted baseline 0% (🔍).**

Multi-tenant `X-Tenant-Id` resolver weight=5 per spec.
